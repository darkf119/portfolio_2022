import React, { useState, useRef } from 'react';

const ThemeSwitcher = () => {

    const [themeOption, SetThemeOption] = useState('theme-light');
    const themeRef = useRef(null);

    const handleThemeColor = () => {

    }

    // const setThemeColor = () => {
    //     localStorage.setItem(`${k}`, theme);
    // }

    return (
        <div>

        </div>
    );
};

export default ThemeSwitcher;
